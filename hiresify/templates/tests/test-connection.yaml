apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hiresify.fullname" . }}-test-connection"
  labels:
    {{- include "hiresify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hiresify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
