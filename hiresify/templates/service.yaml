apiVersion: v1
kind: Service
metadata:
  name: {{ include "hiresify.fullname" . }}
  labels:
    {{- include "hiresify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hiresify.selectorLabels" . | nindent 4 }}
